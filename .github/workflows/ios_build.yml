# DISABLED - workflow này bị xóa vì xung đột với build_ios.yml
# Nó chạy 'flutter create .' và xóa mất toàn bộ iOS Swift source code
on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'Workflow này đã bị vô hiệu hóa'
        required: false
