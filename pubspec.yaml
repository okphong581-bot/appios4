name: hoangha_optimizer
description: HoangHa Sensitivity Optimizer for iOS
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_riverpod: ^2.5.1
  device_info_plus: ^10.1.0
  battery_plus: ^6.0.1
  url_launcher: ^6.2.6
  shared_preferences: ^2.2.3
  glassmorphism: ^3.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
